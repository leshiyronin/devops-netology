1)ok

2)ok

3)Пока странные проблемы powershell - он не видит openssh, в отличие от cmd

4)ок, но пришлось долго искать подходящую машину + образ не скачивается без впн

5)2 CPU, 1024m RAM, 64 VMDK

6)Могу поменять настройки через гуй, могу через vagrantfile, которым изначально поднимал ВМ

7)В мануале bash несколько тысяч строк, искать по нему не было смысла. Тк history - это отдельная утилита, то вызвал

man history
/length

собственно переменная history_length нам и была нужна. 258 строка (пришлось сделать -N в manдля включения нумерации)

8)Как я понял - это опция, позволяющая одновременно использовать ignorespace и ignoredups - те не записывать в историю
команды, написанные после пробела и команды, дублирующие те, что уже выполнялись ранее

9)Судя по строке 257 man bash такие скобки используются для работы со списками команд, позволяя запускать их внутри нынешней командной оболочки.

626 - скобки используются для вызова конкретного символа в строке или значения в списке ${var[]}

1091 - brace expansion  - генерация имен через добавление генерируемых перечислением символов или добавление слов-символов-других значений из списка. Например touch file_{1..7} или echo 'hello,' {world, bash, os}

10)Поправил

100000 файлов я смогу создать, 

300000 - argument too long. Работает ограничение ARG_MAX, его можно проверить или изменить через xargs или getconf
В моем случае xargs --show-limits и getconf ARG_MAX выводят 2090690 байт - 2Mb вместе с уже занятыми 2207 байтами, получается


11)проверяет наличие директории /tmp

Если я верно понял - то это именно проверка для скриптов. В cmd у меня запустить эту конструкцию не получилось.

Зато работает скриптик

if [[ -d /tmp ]]
then
	echo 1
else
	echo 0
fi

Выводит 1

12)Поправил, нужно было просто сделать echo $PATH чтобы осознать, как именно она редактируется. Красивое. 
Теперь пойду читать, почему именно двоеточие, а не другой разделительный символ.

ln /usr/bin/bash /tmp/new_path_directory/bash 
export PATH=/tmp/new_path_directory/:$PATH
type -a bash

13)Мне не нравится мануал по batch - не очень ясны кейсы, когда эту команду лучше использовать. Что если загруженность системы до 0.8 не наступит в течении долгого периода? Для каких задач такая команда уместна?

А так 

at - отложенный одноразовый запуск команды в указанное время
batch - отложенный одноразовый запуск команды в период понижения нагрузки на систему.
